# Parametric 3D Modeling Repository Template

A template for build123d-based parametric modeling projects: a Python `src/`-layout package with pytest tests, a box example that exports STEP files to `manufacture/`, and a `setup.sh` that bootstraps the environment.

See [GETTINGSTARTED.md](GETTINGSTARTED.md) to derive a new repository from this template and start modeling.
